<Window x:Class="M012.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:local="clr-namespace:M012"
		DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="MainWindow" Height="450" Width="800"
		WindowStartupLocation="CenterScreen"
		Topmost="True">
    <StackPanel>
		<!--Commands-->
		<!--Eigene Klasse, welche Logik in Form von Methoden enthält-->
		<!--Commands werden per Binding in UI-Komponenten eingebunden-->
		<!--MVVM kann keine Events benutzen -> Commands-->
		<!--Über CommandParameter können Commands Daten empfangen-->
		<TextBox Name="Eingabe"/>
		<Button Command="{Binding CustomExitCmd}"
				CommandParameter="{Binding ElementName=Eingabe, Path=Text}"
				Content="Programm beenden"/>
	</StackPanel>
</Window>
